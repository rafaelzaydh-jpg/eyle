{
  "release": "2.7.5",
  "app_version": "2.7.5",
  "config_schema_version": "2.7.5-r1.3",
  "revision": "rev1.3-task-memory",
  "released_at": "2026-08-12",
  "title": "Rev1.3 Task Memory",
  "compatibility": {
    "backward_compatible": false,
    "session_schema": "2.7.5-r1.3",
    "queue_schema": "2.7.5-r1.3",
    "project_memory_schema": "2.7.5-r1.3",
    "pending_continuation_schema": "1",
    "benchmark_schema": "1",
    "policy": "No compatibility bridges, aliases or migrations for prior Core contracts. Rev1.3 reserves Task for Main-owned intentional state and uses execution_id for the physical run reference."
  },
  "architecture": {
    "main": "Sole task-semantic authority. Owns interpretation, relevance, Investigation, recursive Tasks, grounding choice, capability choice, recovery and stopping.",
    "runtime": "Physical/contract authority. Executes capabilities and persists structurally valid Investigation/Task state, while enforcing schemas, permissions, sandboxing, transactions, workspace state and physical containment without inferring semantic completion.",
    "observation": "Generic physical boundary containing domain-neutral mat-* Material (`locator`, `content_hash`, optional opaque `source_version`), mechanically validated Coverage, public fr-* Frontiers, and Runtime-private shared snapshots/lightweight continuation cursors. Snapshot payload is stored once and only requested slices are copied during pagination. Observation contains no public-capability or filesystem-specific extraction, freshness, identity or continuation logic.",
    "claim": "Small adversarial critic over Request + provisional Final + selected physical material. Claim cannot plan, call tools, rewrite Final, or mutate Investigation/Tasks.",
    "writes": "Real workspace writes use one confirmation-gated WriteTransaction path. Investigation status is not a write-permission gate; sandbox mutations never authorize real writes.",
    "model_window": "Current llama-server physical context ceiling is 38000 tokens per call. Cumulative prompt/completion budgets and fixed turn/call/tool quotas are absent; the task-wide physical token fuse is capped at 90000 plus the deadline.",
    "capabilities": "Every public registry entry owns execution plus identity/memoization, Material, canonical Coverage/Frontier, continuation/freshness, normalization and public/model projection hooks. Generic dispatch contains no capability-name branches. One effect=observe|execute|mutate field replaces redundant category/effects metadata.",
    "operational_feedback": "Bounded factual projection of recent observable consequences. It complements Task state but does not diagnose loops, close Tasks or prescribe strategy.",
    "transport": "LLM transport records physical request start separately from preflight, preserves backend failure status such as read_timeout/model_output_truncated, and uses the real ExecutionContext for stream eligibility/progress. Structured response validation remains fail-closed.",
    "sandbox": "Physical execution backend boundary. backend=auto prefers embedded Microsandbox microVMs, then Docker, then Bubblewrap. Microsandbox is pinned to 0.6.8 and Runtime bootstraps its local msb/libkrunfw payload. Normal run_command uses Network.from_profiles(\"public\"); explicitly isolated supervised execution uses Network.none(). On Linux/macOS the disposable Eyle snapshot is bind-mounted writable for speed. On native Windows, Runtime deliberately avoids Microsandbox bind mounts because the 0.6.8 passthrough/virtio-fs path can surface valid operations as EACCES/ELOOP; it instead stages the disposable snapshot into the VM-private rootfs through Sandbox.fs.copy_from_host. One microVM persists per physical job; the real workspace is never mounted writable.",
    "tasks": "Main-owned intentional memory. Each Task is exactly id,parent_id,description,status(open|completed|dropped),result. Runtime validates parent integrity/cycles and persistence only; tree position never defines execution order, child closure never closes a parent, and open Tasks never gate Final.",
    "investigation": "Main-owned epistemic notebook answering what Main is trying to understand. It remains separate from Tasks; similar persistence mechanics do not merge semantic ownership."
  },
  "removed": [
    "SourceRecordLedger and source_record.py",
    "EvidenceLedger and evidence.py",
    "src-* to ev-* promotion protocol",
    "Projection as a Core contract",
    "Main-visible opaque continuation handles",
    "expand_observation public tool",
    "agent_info public tool",
    "execution_trace public tool",
    "Claim authority to mutate/reopen Investigation",
    "Rev5.x revision-specific contract tests",
    "NO_GROUNDED_STATE automatic Claim bypass",
    "Runtime semantic strategy guidance and RESOURCE_PRESSURE advice",
    "OBSERVATION_REPLAY_LOOP task-fatal behavior; replay is memoization/telemetry",
    "Investigation status as a Final/write completion gate",
    "Investigation goal immutability and semantic grounding/reason requirements for established/dismissed",
    "Claim material_satisfaction/answer_consistency/semantic_gaps protocol; Claim is accept|challenge + issues",
    "Specialized repeated invalid-Investigation and patch-dry-run semantic fatal counters",
    "Cumulative max_prompt_tokens and max_completion_tokens budgets",
    "Hidden 32768-token secondary context cap",
    "Fixed max_llm_turns, max_llm_calls and max_tool_calls quotas",
    "Observation.material_candidates_from_tool and tool-specific Observation signatures",
    "Agent capability-specific model/presentation branches",
    "file/file_hash as the universal Material identity requirement",
    "Claim access to Investigation and automatic Investigation grounding injection",
    "Claim-owned filesystem freshness validation",
    "DecisionLedger required_properties, rejection fingerprints and repeated-rejection counters",
    "obsolete Claim investigation-target telemetry",
    "Frontier handles that duplicate full continuation payloads; payload is stored once in Runtime-private snapshots",
    "source_hash as Material-wide provenance naming; source_version is the opaque domain-neutral version token",
    "filesystem freshness/rehydration logic inside Observation",
    "implicit missing capability observation/Coverage hook ownership; every registry entry now owns the physical hook surface",
    "whole-snapshot deepcopy during Frontier pagination; only the requested page slice is detached",
    "permissive arbitrary-dict Coverage; non-empty Coverage is now mechanically normalized/validated",
    "find_symbol 32-result silent boundary; exhaustive scan Coverage now fronts bounded materialization with Frontier continuation",
    "central capability-name dispatch for public/model projections, normalization and covering lookup",
    "redundant category + effects registry metadata; effect is the single physical classification",
    "Claim runtime compaction knowledge of code/file/symbol-specific result keys",
    "continuation completeness that conflated snapshot exhaustion with source materialization success",
    "experimental 1,000,000-token default task fuse; Rev1.2.3 caps the physical task envelope at 90,000 tokens",
    "configurable agent.claims.verifier.max_tokens magic ceiling; Claim output reservation is derived from its bounded canonical schema",
    "8-issue/unbounded-reference Claim artifact; Claim now returns at most 3 independent blockers with at most 4 coordinates each",
    "telemetry inference that mislabeled backend timeouts as preflight_blocked when no response metadata existed",
    "conversation-as-task assumption that encouraged needs_user for greetings and other ordinary interaction",
    "provider-specific sandbox.imagem_docker configuration name; current Core uses backend-neutral sandbox.imagem_oci",
    "AgentSession.task_id physical-run naming; the canonical field is execution_id",
    "agent response envelopes without required task_updates",
    "using Investigation as the intentional task list; Tasks are a separate contract"
  ],
  "public_tools": [
    "calculate",
    "project_stats",
    "count_tokens",
    "inspect_project",
    "list_tree",
    "search_code",
    "symbol_relations",
    "continue_observation",
    "find_symbol",
    "read_file",
    "run_command",
    "memory_search",
    "memory_store",
    "run_tests",
    "git_status",
    "git_diff"
  ],
  "agent_action_kinds": [
    "tool_calls",
    "patches",
    "needs_user",
    "final"
  ],
  "publication": {
    "target_tag": "v2.7.5-rev1.3",
    "status": "artifact_verified",
    "requires_extracted_artifact_verification": true
  }
}
