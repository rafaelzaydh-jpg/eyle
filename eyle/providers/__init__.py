"""Capability providers bundled with the default Eyle distribution.

Providers own domain mechanics. Eyle Core does not import provider modules
except through the capability registry assembly boundary.
"""
