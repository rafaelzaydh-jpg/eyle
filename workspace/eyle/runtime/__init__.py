"""Execution infrastructure around the AgentSession core.

The runtime owns queueing, process isolation, persistence, telemetry and the
application service. It does not interpret user intent or reason about code.
"""
