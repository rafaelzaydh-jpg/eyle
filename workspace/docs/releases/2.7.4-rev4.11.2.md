# Eyle 2.7.4 Rev4.11.2 — Write-loop and token fix

Rev4.11.2 keeps the single-AgentSession architecture and repairs the model-facing write protocol. It adds full-file replacement, normalizes common patch shapes, derives hashes from fresh evidence, preserves source for one correction, caps invalid dry-runs, and reduces repeated prompt/output budgets.

Validation: 94 passed, 1 optional Flask test skipped. Real Qwen validation remains deployment-only.
