# Eyle 2.7.4 Rev4.11.4 — Factual response quality

Rev4.11.4 makes project analysis traceable without recreating the removed multi-agent bureaucracy. Project facts, verified bugs, and contextual risks now require real read evidence through an internal typed claim ledger. Bugs, risks, recommendations, and facts remain distinct; explicit “up to N” overall and per-kind limits are enforced by the runtime; and self-correcting lists are rejected before publication.

The active session now retains a bounded set of recent relevant source snippets across intervening tool calls. Final execution details record exactly which files and line ranges support each accepted claim.

Validation: 110 passed, 1 optional Flask test skipped. Real Qwen validation remains deployment-only.
